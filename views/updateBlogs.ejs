<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>update Blogs</title>
    <link rel="stylesheet" href="/css/all.min.css">
    <link rel="stylesheet" href="/css/update.css">
    
<!-- Google tag (gtag.js) -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-V9QYPB859W"></script>

</head>
<body>
    <header>Update or delete faulty blogs</header>

<main>
    <% blogs.forEach((blog)=>{%> 
        <div class="blog" id="<%=blog._id %>">
            <h1><%= blog.tittle %></h1>
            <form action="/update_blog" method="post">
                <input type="text" readonly="true" name="_id" value="<%=blog._id %>" required="true">
                <label for="imgsrc">The source of the image</label>
                <input type="text" name="imageSrc" id="imgsrc" required="true" value="<%=blog.imageSrc %>">

                <label for="contentImage">content image</label>
                <input type="file"  accept="image/*" class="addImage">
                <input type="text" id="contentImage" name="contentImageUrl" placeholder="add image to generate url..." required="true"  value="<%=blog.contentImageUrl %>">

                <label for="searchKeywords">search Keywords</label>
                <input type="text" name="searchKeywords" id="searchKeywords" required="true" placeholder="search engine keywords for better ranking..." value="<%=blog.searchKeywords%>">

                <label for="firstparagraph">first paragraph</label>
                <textarea name="firstParagraph" id="firstparagraph" cols="30" rows="10" required="true"><%-blog.firstParagraph%></textarea>
    
              
                <label for="other">other(if you have extra content you can insert here but use html tags to add other content)</label>
                <textarea name="other" id="other" cols="100" rows="10"><%-blog.other%></textarea>
                <button type="submit" id="update_blog">Update blog</button>
            </form>
            <button id="delete_blog" class="delete_blog">Delete blog</button>
        </div>
        <%})%>
</main>
<script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
<script src="./javascript/update.js"></script>
</body>
</html>